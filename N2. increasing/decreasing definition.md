 ## N2. increasing/decreasing definition

A1. Define “$f$ is increasing on $A$” using quantifiers (strict version).

A2. Define “$f$ is decreasing on $A$” using quantifiers (strict version).

A3. Define “$f$ is nondecreasing on $A$” and “nonincreasing on $A$.” What symbol changes relative to A1–A2?

A4. Define “$f$ is one-to-one on $A$” using quantifiers.

B1. What does it mean for $A\subset\mathbb R$ to be an interval? State the “between” property.

B2. Give an example of a domain that is “two or more intervals” written as a union, e.g. $A=I_1\cup I_2$.

B3. If $A$ is a union of two disjoint intervals, what does “$f$ is increasing on $A$” mean formally? (Be explicit about the quantifiers ranging over all of $A$.)

B4. True/false with reason: if $f'(x)>0$ for all $x\in A$ and $A$ is not an interval, then $f$ must be one-to-one on $A$.

G1. Prove: if $f'(x)>0$ on an interval $I$, then $f$ is one-to-one on $I$. (Max 3 lines, must name the theorem used.) 



